RUN apt-get install -y git-all

RUN apt-get install -y cmake && apt-get upgrade -y

RUN git clone https://github.com/3dem/relion.git

RUN cd relion && mkdir build && cd build && cmake .. && make -j 2

ENV PATH ~/relion/build/bin:$PATH

